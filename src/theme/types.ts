export interface ThemeColors {
  bg: string;
  surface: string;
  text: string;
  muted: string;
  primary: string;
  onPrimary: string;
  border: string;
  track: string;
  tight: string;
  good: string;
  loose: string;
  overlay: string;
  subText: string;
}

export interface ThemeTokens {
  name: string;
  radius: string;
  font: string;
  colors: ThemeColors;
}
